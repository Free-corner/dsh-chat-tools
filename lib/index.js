/**
 * dsh-chat-tools — node half.
 *
 * Deliberately empty: this package is a browser-only (client) plugin. The
 * node half exists so the package registers cleanly in the profile; all UI
 * lives in lib/client.js.
 */

/** Host plugin body — nothing to do on the host side. */
export function apply() {}
